# CORS dihandle di PHP file langsung (login.php, dll)
# Jangan set CORS di .htaccess biar ga konflik

<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirect to public folder
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
